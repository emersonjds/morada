import React from "react";
import { View } from "react-native";
import { Box } from "../../components/Spacing";

// import { Container } from './styles';

const Tickets: React.FC = () => {
  return (
    <>
      <Box flex={1} bg={#fff} padding={20}></Box>
    </>
  );
};

export default Tickets;
